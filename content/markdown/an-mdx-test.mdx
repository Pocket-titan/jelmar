---
date: 2022-10-19
title: test
---

# hello my frends

i am a test $will this be math?$

```py
code
```

<div>Hello</div>

So this math should display <Math inline>`\frac{1}{2} \Sigma_0^{\infty} \int_0^{2\pi} \phi(i,\ j)\, dx`</Math> inline? And it does!

<Math>
  `\label{eq:myeq}
  f(x) = \frac{1}{2}`
</Math>

later on... <Math inline> `hi? \ref{eq:myeq}`</Math>

some custom code:

<Code language="js">
  {`import React from 'react';

const App = () => {

}`}

</Code>

snake tongue?

```py
import numpy as np

def funcy(x):
  return x**2
```

but if I want a small code `like this`....

$$
\begin{align}
  \tag{lift}\label{eq1}
  L &= \frac{1}{2} \rho v^2 S C_L \\
  a &= b + c
\end{align}
$$

in equation $\ref{eq1}$ we see that...

$$
\tag{hi} x+y^{2x} \label{tag1}
$$

```jsx
const App = ({ children, ...props }) => {
  return (
    <div>
      <h1>hi</h1>
      <Content key={`hello-${props.id}`} className="className-test">
        {children}
      </Content>
    </div>
  );
}
```

<Note>
  hello!
</Note>
